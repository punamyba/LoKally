import MapView from "./MapView";

const ExploreMap = () => {
  return (
    <div style={{ height: "100vh", width: "100vw" }}>
      <MapView fullHeight />
    </div>
  );
};

export default ExploreMap;
